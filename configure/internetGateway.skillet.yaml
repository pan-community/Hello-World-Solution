name: panos_layer3_config_group
label: PAN-OS layer 3 config group
description: |-
  group of snippets for layer 3 internet gateway configuration
type: panos
labels:
  collection:
    - Hello World

snippets:

  # IronSkillet baseline
  # TODO: pull in snippets when ready
  # current load requires zone protect profiles, profiles, groups from IronSkillet

  # demo specific config items different from day one IronSkillet settings
  - name: demo_config_snippets
    include: panos_demo_snippets_device_admin
    include_snippets:
      - name: panos_idle_timeout_60min

  # layer3 internet gateway snippets
  - name: layer3_config_group
    include:
      - name: panos_network_snippets_layer3
      - name: panos_network_snippets_dhcp_server_basic
      - name: panos_network_snippets_nat_dipp
      - name: panos_policy_snippets_outbound_security_rules

